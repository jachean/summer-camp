{% extends 'base.html.twig' %}

{% block title %}Edit Matches{% endblock %}

{% block body %}
    <h1>Edit Matches</h1>

    {{ include('matches/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_matches_index') }}"><button type="button" class="btn btn-success">Back to List</button></a>

    {{ include('matches/_delete_form.html.twig') }}
{% endblock %}
